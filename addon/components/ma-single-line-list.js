import Ember from 'ember';
import layout from '../templates/components/ma-single-line-list';

export default Ember.Component.extend({
  classNames: ['ma-single-line-list'],
  tagName: 'ul',
  
  layout
});
